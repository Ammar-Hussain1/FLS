<UserControl x:Class="FLS.TimetableUploadView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FFEDDF">

    <UserControl.Resources>
        <SolidColorBrush x:Key="Primary" Color="#2B2D42"/>
        <SolidColorBrush x:Key="Surface" Color="#FFEDDF"/>
        <SolidColorBrush x:Key="Accent" Color="#4281A4"/>
        <SolidColorBrush x:Key="Success" Color="#48A9A6"/>
        <SolidColorBrush x:Key="Warning" Color="#FFA500"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{StaticResource Primary}"
                Padding="30,25">
            <StackPanel>
                <TextBlock Text="📅 Timetable Upload"
                          FontSize="32"
                          FontWeight="Bold"
                          Foreground="White"/>
                <TextBlock Text="Upload timetable files for parsing and processing"
                          FontSize="14"
                          Foreground="White"
                          Opacity="0.8"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="30,25">
            <StackPanel>
                <!-- Upload Section -->
                <Border Background="White"
                       CornerRadius="12"
                       Padding="30"
                       Margin="0,0,0,20">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" 
                                         Opacity="0.1" 
                                         BlurRadius="15" 
                                         ShadowDepth="3"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Title -->
                        <TextBlock Grid.Row="0" 
                                  Text="Upload New Timetable"
                                  FontSize="22"
                                  FontWeight="Bold"
                                  Foreground="{StaticResource Primary}"
                                  Margin="0,0,0,20"/>

                        <!-- File Selection Area -->
                        <Border Grid.Row="1"
                               BorderBrush="{StaticResource Accent}"
                               BorderThickness="2"
                               CornerRadius="10"
                               Padding="40"
                               Background="#F8F9FA"
                               Margin="0,0,0,20">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="📁"
                                          FontSize="48"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,15"/>
                                
                                <TextBlock Name="SelectedFileText"
                                          Text="No file selected"
                                          FontSize="16"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource Primary}"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Supported formats: .xlsx, .csv, .txt"
                                          FontSize="13"
                                          Foreground="Gray"
                                          HorizontalAlignment="Center"
                                          Margin="0,0,0,20"/>
                                
                                <Button Name="BrowseButton"
                                       Content="📂 Browse Files"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Background="{StaticResource Accent}"
                                       Foreground="White"
                                       BorderThickness="0"
                                       Padding="30,12"
                                       Cursor="Hand"
                                       Click="BrowseButton_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="{StaticResource Accent}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                               CornerRadius="8"
                                                               Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center"
                                                                            VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#5BA3C9"/>
                                                </Trigger>
                                                <Trigger Property="IsPressed" Value="True">
                                                    <Setter Property="Background" Value="#3A7A9E"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>

                        <!-- File Details (Hidden by default) -->
                        <Border Grid.Row="2"
                               Name="FileDetailsPanel"
                               Background="#E8F4F8"
                               BorderBrush="{StaticResource Accent}"
                               BorderThickness="1"
                               CornerRadius="8"
                               Padding="20"
                               Margin="0,0,0,20"
                               Visibility="Collapsed">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0"
                                          Text="File Name:"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource Primary}"
                                          Margin="0,0,15,8"/>
                                <TextBlock Grid.Row="0" Grid.Column="1"
                                          Name="FileNameText"
                                          FontSize="14"
                                          Foreground="{StaticResource Primary}"
                                          Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="1" Grid.Column="0"
                                          Text="File Size:"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource Primary}"
                                          Margin="0,0,15,8"/>
                                <TextBlock Grid.Row="1" Grid.Column="1"
                                          Name="FileSizeText"
                                          FontSize="14"
                                          Foreground="{StaticResource Primary}"
                                          Margin="0,0,0,8"/>

                                <TextBlock Grid.Row="2" Grid.Column="0"
                                          Text="File Type:"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          Foreground="{StaticResource Primary}"
                                          Margin="0,0,15,0"/>
                                <TextBlock Grid.Row="2" Grid.Column="1"
                                          Name="FileTypeText"
                                          FontSize="14"
                                          Foreground="{StaticResource Primary}"/>
                            </Grid>
                        </Border>

                        <!-- Upload Button -->
                        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Name="ClearButton"
                                   Content="🗑️ Clear"
                                   FontSize="15"
                                   FontWeight="SemiBold"
                                   Background="#6C757D"
                                   Foreground="White"
                                   BorderThickness="0"
                                   Padding="25,10"
                                   Margin="0,0,15,0"
                                   Cursor="Hand"
                                   Click="ClearButton_Click"
                                   Visibility="Collapsed">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                           CornerRadius="8"
                                                           Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#5A6268"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>

                            <Button Name="UploadButton"
                                   Content="⬆️ Upload Timetable"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   Background="{StaticResource Success}"
                                   Foreground="White"
                                   BorderThickness="0"
                                   Padding="30,12"
                                   Cursor="Hand"
                                   Click="UploadButton_Click"
                                   IsEnabled="False">
                                <Button.Style>
                                    <Style TargetType="Button">
                                        <Setter Property="Background" Value="{StaticResource Success}"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="Button">
                                                    <Border Background="{TemplateBinding Background}"
                                                           CornerRadius="8"
                                                           Padding="{TemplateBinding Padding}">
                                                        <ContentPresenter HorizontalAlignment="Center"
                                                                        VerticalAlignment="Center"/>
                                                    </Border>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#5BC0BE"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter Property="Background" Value="#3A8A87"/>
                                            </Trigger>
                                            <Trigger Property="IsEnabled" Value="False">
                                                <Setter Property="Background" Value="#CCCCCC"/>
                                                <Setter Property="Foreground" Value="#666666"/>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Upload History Section -->
                <Border Background="White"
                       CornerRadius="12"
                       Padding="30">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" 
                                         Opacity="0.1" 
                                         BlurRadius="15" 
                                         ShadowDepth="3"/>
                    </Border.Effect>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" 
                                  Text="📋 Upload History"
                                  FontSize="22"
                                  FontWeight="Bold"
                                  Foreground="{StaticResource Primary}"
                                  Margin="0,0,0,20"/>

                        <!-- History Table -->
                        <DataGrid Grid.Row="1" 
                                 Name="UploadHistoryGrid"
                                 AutoGenerateColumns="False" 
                                 IsReadOnly="True" 
                                 CanUserAddRows="False" 
                                 CanUserDeleteRows="False" 
                                 CanUserResizeRows="False" 
                                 HeadersVisibility="Column" 
                                 GridLinesVisibility="Horizontal" 
                                 BorderBrush="{StaticResource Accent}" 
                                 BorderThickness="1" 
                                 RowHeight="45"
                                 MinHeight="200">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="File Name" Binding="{Binding FileName}" Width="*" MinWidth="200"/>
                                <DataGridTextColumn Header="Upload Date" Binding="{Binding UploadDate, StringFormat='{}{0:MM/dd/yyyy HH:mm}'}" Width="150"/>
                                <DataGridTextColumn Header="File Size" Binding="{Binding FileSize}" Width="100"/>
                                <DataGridTemplateColumn Header="Status" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Center">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding Status}" Value="Uploaded">
                                                                <Setter Property="Background" Value="{StaticResource Success}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Processing">
                                                                <Setter Property="Background" Value="{StaticResource Warning}"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="Completed">
                                                                <Setter Property="Background" Value="#28A745"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold"/>
                                            </Border>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="Uploaded By" Binding="{Binding UploadedBy}" Width="150"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
